// Stubbed — gateway news removed

import { createMemo, Show } from "solid-js"
import type { KilocodeNotification } from "./news"

export function KiloNews(_props: { items: KilocodeNotification[] }) {
  return null
}
