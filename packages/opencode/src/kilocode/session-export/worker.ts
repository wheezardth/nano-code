// Session export worker stub — session-export functionality removed with gateway dependency.
// This file exists as a build entrypoint so the CLI compiles.
self.addEventListener("message", () => {})
